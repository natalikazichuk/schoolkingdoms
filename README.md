# 🏰 SchoolKingdoms

**Гейміфікована освітня платформа для дошкільнят і школярів** — українською мовою.
Дитина вчиться через тести, тренажери, ігри, читання та словник, а її герой росте:
характеристики, монети, спорядження, бої на Турнірній арені. Батьки бачать усе з власного кабінету.

> Ukrainian-language gamified learning platform for kids. A build-less static site on GitHub Pages, powered by Firebase (Auth + Firestore).

🌐 **Демо:** https://natalikazichuk.github.io/schoolkingdoms

---

## 📚 Зміст

- [Що всередині](#-що-всередині)
- [Ролі та вхід](#-ролі-та-вхід)
- [Мапа сторінок](#-мапа-сторінок)
- [Спільний шар](#-спільний-шар)
- [Дані у Firestore](#-дані-у-firestore)
- [Адмінка](#-адмінка)
- [Структура репозиторію](#-структура-репозиторію)
- [Локальний запуск](#-локальний-запуск)
- [Деплой](#-деплой)
- [Домовленості розробки](#-домовленості-розробки)
- [Медіа: картинки й озвучка](#-медіа-картинки-й-озвучка)
- [Технології](#-технології)
- [Copyright](#-copyright)

---

## ✨ Що всередині

| Розділ | Що це |
|--------|-------|
| **Навчання** | Два ступені: 🌱 «Дошкільнятко» і «Молодша школа» (1 клас; 2–4 — заділ). Плитки предметів відкриваються, щойно в предметі зʼявляється контент. |
| **Тести** | Дорожня карта тестів королівства + універсальний плеєр `test.html`: питання з бази, рівні, ілюстрації (числовий промінь, кружечки, картинка), нагорода характеристиками. |
| **Тренажери** | Абетка, «Вчимо літери», «Вчимо цифри», «Читаємо склади», «Learn ABC!», уроки читання англійською, тварини, письмо по пунктиру, тексти й списування. |
| **Словник English** | `slovnyk-en.html`: набори слів із адмінки — флеш-картки, перевірка, «збери слово з літер», озвучка й картинки. |
| **Ігри** | Понад 20 міні-ігор: лічба й числа, задачі, час, гроші, геометрія, ноти, логіка, памʼять, шахи, шашки, хрестики, змійка, забіг. |
| **Бібліотека** | Читацький щоденник + читалка книжок (`biblioteka.html`, `book.html`). |
| **Герой** | Характеристики (здоровʼя, влучність, спритність, мана), аватар, монети, завдання від батьків, «Моя команда» (друзі). |
| **Арена** | Турнірна арена: таверна, двобій 1×1, прогрес і рейтинг, зала слави; загін 3×3, турнір і бій за титул — у роботі. |
| **Спорядження** | Інвентар і екіпіровка: предмети з каталогу адмінки, випадковий бонус екземпляра, міцність, ціна. |
| **Кабінет батьків** | Огляд, створення й видача завдань, перевірка виконаного, статистика, керування Героями, друзі та дерево роду. |
| **🐞 Помилки** | Кнопка «Повідомити про помилку» на кожній сторінці → лист підтримці; усі повідомлення видно в адмінці. |

Сайт ставиться на телефон як **PWA** (`manifest.webmanifest`, standalone, портрет).

---

## 👤 Ролі та вхід

Вхід починається з `index.html`: **«Вхід — user»** (батьки) або **«Вхід — Герой»** (дитина).

- **Батьки** — звичайний акаунт Firebase Auth (email + пароль), реєстрація родини.
- **Герой** — окрема дитяча сесія на синтетичному email (`login@hero.schoolkingdom.app`), логін і пароль створює дорослий.
- **Адміністратор** — батьківський акаунт із прапорцем адміна (`SK.isAdmin()`), бачить кнопку «⚙️ Адмінка».

Сесія тримається через `browserSessionPersistence` — до закриття вкладки.
Щоб тестувати Героя й батьків одночасно, потрібні різні браузери або вікно інкогніто.

---

## 🗺 Мапа сторінок

```
index.html ─┬─ login.html / auth.html ─┬─ hero.html ──── karta.html
            │                          │      ├── navchannia.html ─┬─ doshkillya.html
            │                          │      │                    └─ tests.html → test.html
            │                          │      ├── igry.html ───── games/ · klas-1/ · doshkilya/
            │                          │      ├── biblioteka.html → book.html
            │                          │      ├── slovnyk-en.html
            │                          │      ├── arena.html
            │                          │      └── inventar.html · ekipirovka.html
            │                          └─ parent.html (кабінет батьків)
            └─ prezentaciya.html (відео-презентація)

admin.html ─┬─ admin-books.html · admin-items.html · admin-vocab.html · admin-reports.html
```

Внизу кожної сторінки Героя — спільний футер `.botnav` (`sk-footer.js`):
🏠 Головна · 🎓 Навчання · 🕹 Ігри · ⚔️ Арена · ⬅️ Назад (або 🚪 Вийти).

---

## 🧩 Спільний шар

Підключається сторінками звичайними тегами — жодних імпортів і збірки.

| Файл | Глобал | Призначення |
|------|--------|-------------|
| `firebase-config.js` | `SK` | Ініціалізація Firebase; уся робота з даними: вхід, Герої, тести, завдання, книжки, предмети, словник, друзі, звіти |
| `sk-curriculum.js` | `SKCUR` | Єдина карта королівства (ступені → класи → предмети → розділи → тести); `DEFAULT_MAP` як запасний варіант, коли база недоступна. Має `API_VERSION` — сторінки перевіряють його |
| `sk-header.js` | — | Спільна шапка: бренд, навігація з карти, характеристики активного Героя |
| `sk-footer.js` | — | Спільний нижній навбар; вкладки `home · navch · games · arena`; налаштування — атрибутами `data-active`, `data-back`, `data-last` на теґу `<script>` |
| `sk-progress.js` | — | Health / точність із виконаних завдань, запис у базу |
| `sk-items.js` | `SKIT` | Рушій предметів: бонус екземпляра, міцність, ціна, сумарні відсоткові навички |
| `sk-avatar.js` | `SKAVA` | Аватари: емодзі, вбудовані герої, локальне фото; модалка вибору |
| `sk-admin.js` + `sk-admin.css` | `SKADMIN` | Спільний шар адмін-сторінок: гейт доступу, шапка, хелпери |
| `sk-report.js` | — | Плаваюча 🐞-кнопка «Повідомити про помилку» |
| `sk-finish.js` | — | Зарахування HTML-сторінки як пройденого запису (`?skdone=t:<testId>`) |
| `sk-zadachi.js` | `ZAD` | Генератор текстових задач (із відмінками) для ігор «Задачі» |
| `sk-styles.css` | — | Спільні стилі: фон, плитки, шапка, футер, адмінка |
| `sk-game.css`, `sk-game-chalk.css` | — | Стилі ігор |

> `firebase-config.js` — ES-модуль, тому `window.SK` зʼявляється **після** `DOMContentLoaded`.
> Не звертайтеся до `SK` одразу: чекайте через `SK.onUser()` або `SKADMIN.whenReady()`.

---

## 🔥 Дані у Firestore

Проєкт Firebase: **`schoolkingdom-d46bc`**.

| Колекція | Що зберігає |
|----------|-------------|
| `users` | Батьківські акаунти, родина, прапорець адміна |
| `heroes` | Героїв: характеристики, клас, аватар, монети, інвентар, завдання |
| `heroCards` | Публічні картки Героїв (для друзів і дерева роду) |
| `tests` | Тести й записи навчання (html-сторінки теж тут) |
| `curriculum` | `curriculum/map` — карта королівства; `curriculum/games` — сітка ігор |
| `books` | Бібліотека |
| `items` | Каталог предметів (IT-XXX) |
| `vocab`, `vocabDecks` | Слова й набори словника English |
| `friendships`, `inviteCodes` | Друзі: запрошення з подвійним підтвердженням |
| `reports` | Повідомлення про помилки |
| `meta` | Службові документи |

Локально в `localStorage` живе те, що не варто гнати в базу: голос озвучки, проходження
(`sk_testpass_<id>`, `sk_dbtest_<id>`, `sk_test100_<id>`), прогрес арени (`sk_arena_*`),
локальне фото аватара (`sk_hero_avatar`).

---

## 🛠 Адмінка

`admin.html` — хаб із пʼятьма розділами (детальна схема — у [`shema-adminky.md`](shema-adminky.md)):

- **📚 Навчання** — ступені → класи → предмети → редактор контенту (HTML-сторінка, тести по шаблону, імпорт/експорт JSON, структура карти).
- **🕹 Ігри** — сітка `igry.html`: додати гру, порядок, групи, імпорт/експорт.
- **⚠️ Помилки** — `admin-reports.html`: статуси, експорт JSON.
- **🎒 Items** — `admin-items.html`: каталог предметів і базові значення.
- **⚔️ Арена** — заділ на майбутнє.

Поруч: **📖 Книги** — `admin-books.html`, **🔤 Словник English** — `admin-vocab.html`.

---

## 📁 Структура репозиторію

```
schoolkingdoms/
├── index.html                    # Вхід: батьки або Герой
├── login.html · auth.html        # Автентифікація
├── prezentaciya.html             # Відео-презентація
│
├── hero.html + hero.css          # Сторінка Героя
├── karta.html                    # Карта королівства
├── inventar.html                 # Інвентар
├── ekipirovka.html               # Екіпіровка (прототип)
├── arena.html                    # Турнірна арена
├── parent.html                   # Кабінет батьків
│
├── navchannia.html               # Хаб «Навчання» (ступені)
├── doshkillya.html               # Хаб «Дошкільнятко»
├── tests.html                    # Дорожня карта тестів
├── test.html                     # Універсальний плеєр тестів
├── slovnyk-en.html               # Словник English
├── biblioteka.html · book.html   # Бібліотека та читалка
├── igry.html                     # Сітка ігор
│
├── doshkilya/                    # Дошкільні тренажери й ігри (11 сторінок)
├── klas-1/                       # Тренажери та ігри 1 класу (28 сторінок)
├── games/                        # Ігри без прив'язки до класу (9 сторінок)
├── learn/                        # Навчальні презентації
│
├── admin.html                    # Адмінка: хаб, навчання, ігри
├── admin-books.html              # Книжки
├── admin-items.html              # Предмети
├── admin-vocab.html              # Словник English
├── admin-reports.html            # Повідомлення про помилки
│
├── firebase-config.js            # Спільний шар (див. вище)
├── sk-*.js · sk-*.css
├── firestore.rules               # Правила доступу
│
├── img/ · img/nav/               # Зображення, фони плиток
├── audio/                        # Озвучка (mp3)
├── docs/                         # Тест-плани, чек-листи, баг-репорти
├── test-*.json                   # Шаблони й приклади тестів
├── shema-adminky.md              # Схема адмінки
└── README.md
```

---

## 💻 Локальний запуск

Збірки немає — досить будь-якого статичного сервера:

```bash
git clone https://github.com/natalikazichuk/schoolkingdoms.git
cd schoolkingdoms

python3 -m http.server 8000     # або: npx serve .
```

Відкрити `http://localhost:8000`.

> ⚠️ Відкрити файл через `file://` недостатньо: Firebase і ES-модулі потребують `http(s)`.

---

## 🚀 Деплой

Сайт публікується **GitHub Pages** з гілки `main`. Будь-який мердж у `main` одразу на проді.
Оновлювати можна і без CLI: **Add file → Upload files → Commit** у веб-інтерфейсі GitHub.

Правила Firestore (`firestore.rules`) публікуються в консолі Firebase **повним файлом** —
частковий файл мовчки забирає доступ до пропущених колекцій.

---

## 🧷 Домовленості розробки

- **Мова:** інтерфейс і контент — українською; код, імена файлів і змінних — англійською (транслітом).
- **Без дублювання:** спільне живе у `sk-*` файлах. Скопійований у сторінку шматок шапки, футера чи плиток рано чи пізно розійдеться з оригіналом — саме так уже ламалися розміри плиток.
- **Самодостатність сторінки:** кожна сторінка — цілісний файл-заміна; спільні контракти (`sk-curriculum.js`, `test.html`, `sk-styles.css`) без потреби не чіпаємо.
- **Нова сторінка** підключає спільний хедер і футер і має робочу кнопку «Назад».
- **Перевірка перед комітом:**
  - баланс дужок CSS: `count('{') == count('}')`;
  - баланс HTML-тегів;
  - `node --check` для інлайн-JS (для ES-модулів: `node --input-type=module --check < file`).
- **`SKCUR.API_VERSION`** інкрементуємо щоразу, коли міняється експорт `sk-curriculum.js`.
- **Версія кеша:** підключення спільних файлів із `?v=N` — після правки збільшуємо N.

---

## 🖼 Медіа: картинки й озвучка

- **Фони плиток** — `img/nav/`: 16:10, 1200×750 px, WebP, до 150 КБ. Імена й запасні варіанти описані в [`img/nav/README.md`](img/nav/README.md).
- **Решта зображень** — `img/` за домовленим іменуванням (`abc_<letter>.webp`, `colors_<n>.jpg`, …).
- **Озвучка** — `audio/` (`tvaryna_<slug>.mp3`, `abc_<letter>.mp3`).
- Якщо файлу немає, код не падає: показує запасну картинку за темою або емодзі-чернетку.

---

## 🧰 Технології

| Шар | Технологія |
|-----|-----------|
| Фронтенд | Ванільні **HTML5 / CSS3 / JavaScript (ES6+)** — без фреймворків і збірки |
| Автентифікація | **Firebase Authentication** |
| База даних | **Cloud Firestore** |
| Хостинг | **GitHub Pages** |
| PWA | `manifest.webmanifest` + іконки 192/512 |
| Шрифти | Fredoka, Nunito, Playfair Display, Comfortaa (Google Fonts) |
| Озвучка | Web Speech API + власні mp3 |

---

## 📄 Copyright

© 2026 Nataliia Kazichuk. All Rights Reserved.

SchoolKingdoms is an original educational platform created and developed by Nataliia Kazichuk.

This repository is provided for portfolio and demonstration purposes only.

No permission is granted to copy, reproduce, modify, distribute, or commercially use any part
of this project without prior written permission from the author.

---

<div align="center">

Зроблено з ❤️ для маленьких героїв.

</div>
